# 日本史 一問一答 学習ツール — 運用ルール

このファイルは Claude が毎回読むルール。ここに書いていないことを勝手にやらない。

## 0. 大前提

- 制作も利用もスマホのみ。ユーザーは PC を一切触れない。
- `npm install` / ビルド / ローカルサーバー起動を前提にした設計は禁止。
- 外部ライブラリ・CDN は使わない。素の HTML / CSS / JS のみ。
- API キー・トークン・個人情報をコードに書かない（public リポジトリ）。
- 用語レベルは難関私大（慶應法）に合わせる。
- ユーザーへの返答は日本語。

## 1. 役割分担

| 担当 | やること |
|---|---|
| Claude | プリント写真の読み取り、`data/questions.json` への追記、傾向分析、`data/history.json`・`data/stats.json` の更新、git push |
| Web アプリ | 出題・自己採点・履歴保存・復習日計算 |

- 写真の読み取りをアプリ側でやらせない。
- 復習日の計算はアプリ側 JS のみ。Claude は同じ計算を二重に持たない（分析とバックアップだけ）。
- ユーザーがチャットで「出題して」と言った時だけ、Claude が補助的に出題役をやる（§6）。

## 2. ファイル構成

```
index.html           HTML/CSS/JS を全部この1ファイルに入れる
sw.js                オフライン用 Service Worker（別ファイルにしてよい唯一の JS）
data/questions.json  問題本体（唯一の正データ）
data/history.json    履歴のバックアップ兼分析用
data/stats.json      成績のバックアップ兼分析用
CLAUDE.md            このファイル
README.md            公開 URL と使い方メモ
```

上記以外に HTML / CSS / JS ファイルを増やさない。プリントの画像自体はコミットしない。

## 3. データ仕様

### 3.1 data/questions.json

```json
{
  "version": 1,
  "updated": "2026-09-05",
  "lastId": "q0000",
  "questions": []
}
```

`questions[]` の1件：

```json
{
  "id": "q0001",
  "出典": "坂本プリント 第3回",
  "時代": "平安時代",
  "単元": "摂関政治",
  "タグ": ["人物", "藤原氏"],
  "問題文": "969年、左大臣源高明が失脚した事件を何というか。",
  "正答": "安和の変",
  "別表記": ["あんなのへん", "安和ノ変"],
  "補足": "藤原氏による他氏排斥の最後。以後、摂関常置が定着する。",
  "難易度": 2
}
```

- `id` は `q0001` 形式の連番。**一度使った id は絶対に再利用しない。** 削除しても欠番のままにする。
- 追記時は `lastId` の次から採番し、書き終えたら `lastId` を更新する。
- `別表記[]` にはかな・漢字・送り仮名・略称を入れる。アプリは答え表示時にこれも並べて出す（自己採点の基準）。
- `補足` は周辺知識 1〜2 行。
- `難易度` は 1〜3（1=基礎 / 2=標準 / 3=難関私大）。
- `タグ` は横断的な観点（例：人物 / 法令 / 年号 / 外交 / 土地制度 / 仏教 / 文化 / 経済）。弱点タグ出題で使う。
- `時代` は通史の区分（例：旧石器 / 縄文 / 弥生 / 古墳 / 飛鳥 / 奈良 / 平安 / 鎌倉 / 室町 / 安土桃山 / 江戸 / 明治 / 大正 / 昭和戦前 / 昭和戦後 / 平成）。表記を毎回そろえる。

### 3.2 data/history.json（1解答 = 1レコード）

```json
{
  "version": 1,
  "updated": "2026-09-05",
  "history": []
}
```

`history[]` の1件：

```json
{ "id": "q0001", "ts": "2026-09-05T12:34:56+09:00", "result": "correct", "mode": "normal", "src": "app" }
```

- `result` … `"correct"` / `"close"`（惜しい） / `"wrong"`
- `mode` … `"normal"` / `"wrong-only"` / `"range"` / `"weak-tag"`
- `src` … `"app"`（アプリ） / `"chat"`（チャット出題モード）

### 3.3 data/stats.json（問題ごとの成績・復習状態）

```json
{
  "version": 1,
  "updated": "2026-09-05",
  "stats": {}
}
```

`stats` は id をキーにしたオブジェクト：

```json
"q0001": {
  "seen": 3, "correct": 2, "close": 0, "wrong": 1,
  "streak": 1, "stage": 1, "graduated": false,
  "lastAnswered": "2026-09-05T12:34:56+09:00",
  "nextDue": "2026-09-08",
  "wrongCount": 1, "critical": false
}
```

- `streak` … 連続正解数 / `stage` … 復習間隔ラダーの位置 / `wrongCount` … 通算誤答数
- `critical` … 通算3回以上間違えた問題（最重要リスト）
- `nextDue` … 次回出題日（`YYYY-MM-DD`、JST）

### 3.4 アプリ内部のキー

- `rekishi_history` … 履歴（§3.2 の `history[]`）
- `rekishi_stats` … 成績・復習状態（§3.3 の `stats`）
- `rekishi_meta` … UI 用の補助情報のみ（前回起動時の総問題数など）。学習データではないので、エクスポート・バックアップの対象外。

### 3.5 エクスポートの形式

アプリの「エクスポート」は、次の1つの JSON を出力する。

```json
{ "exportedAt": "2026-09-06T02:00:00+09:00", "dataVersion": 31, "history": [ ... ], "stats": { ... } }
```

Claude はこれを受け取ったら `history` を `data/history.json` の `history[]` に、`stats` を `data/stats.json` の `stats` にマージして、両ファイルの `updated` を当日（JST）に更新してコミットする。

### 3.6 更新経路は1本だけ

```
学習中の正データ = localStorage（キー: rekishi_history / rekishi_stats）
   ↓ アプリの「エクスポート」
ユーザーがチャットに貼る
   ↓ Claude が data/history.json・data/stats.json を更新してコミット
リポジトリ（バックアップ兼分析用）
   ↓ アプリの「インポート」ボタンのみ（機種変更・復元用）
localStorage
```

- アプリがリポジトリのファイルを直接書き換えることはない。
- Claude が localStorage を触ることはない。
- 貼られたエクスポートは既存とマージする（同じ `id`+`ts` は重複扱いで捨てる）。`stats` は貼られた側を新しいものとして採用。

## 4. /add-print（プリント追加）— 最重要

ユーザーが「/add-print」と言って写真を送ってきたときの手順。

1. 写真を読み取る。抜き出す優先順位は **太字 → 赤字 → 空欄 → 年号 → 人物名 → 法令名**。
2. **追記のみ。** 既存データを消さない・作り直さない・並べ替えない。差分追加だけ。
3. 追記前に重複チェック（同じ `正答` かつ似た `問題文`）。重複していたら新規追加せず、既存レコードの `出典` に「/ 第N回」の形で追記するだけ。
4. public リポジトリなので、プリントの文面をそのまま写さない。必ず一問一答の形に書き直す。画像はコミットしない。
5. 読めなかった箇所は勝手に補完しない。該当フィールドに `[要確認]` を付けて、報告の中でも明示する。
6. `id` は `lastId` の次から連番。書き終えたら `lastId` を更新。
7. `version` を +1、`updated` を当日（JST）に更新して push。
8. 追加した問題を一覧でユーザーに提示し、確認を求める（何問追加 / どの時代・単元 / `[要確認]` は何件）。

## 5. 出題モード（アプリ・チャット共通）

1. **通常** … 未出題 ＋ `nextDue` が今日以前のものを混ぜる
2. **間違えた問題のみ** … 直近誤答 ＋ `streak < 2`
3. **範囲指定** … `時代` / `単元` / `出典`（プリント回）で絞る
4. **弱点タグ** … タグ別正答率の低い順に集中出題

## 6. チャット出題モード（補助）

- 1問ずつ。答えを先に出さない。
- ユーザーが答えたら ○/× と正答＋補足1〜2行。
- 表記ゆれ（かな・漢字・送り仮名・略称）は正解扱い。`別表記[]` を基準にする。
- 一部だけ合っている場合は「惜しい」として別記録。
- セッション終了時に、その分の履歴をアプリにインポートできる形式（§3.2 の `history[]` 配列、`src: "chat"`）でまとめて出力する。

## 7. 復習アルゴリズム（アプリ側 JS に実装する仕様）

間隔ラダー `LADDER = [1, 3, 7]`（日）。判定後に以下を適用する。

- **不正解** … `streak=0`, `stage=0`, `wrongCount+1`, `graduated=false`, `nextDue = 今日 + 1日`
- **惜しい** … `streak=0`, `graduated=false`, `nextDue = 今日 + ceil(LADDER[stage] / 2)日`（stage は据え置き）
- **正解** … `streak+1`
  - `streak >= 2` → `graduated=true`, `nextDue = 今日 + 30日`（月1回の抜き打ち）
  - それ以外 → `stage = min(stage+1, 2)`, `nextDue = 今日 + LADDER[stage]日`
- **卒業後に間違えたら** `graduated=false` に戻し、上のルールをそのまま適用する。
- `wrongCount >= 3` の問題は `critical=true`（最重要リスト）。卒業していても通常モードで優先的に混ぜる。
- 日付は JST の日単位で扱う（時刻は切り捨て）。

## 8. アプリの画面

- **ホーム** … 4モードの大きなボタン ＋「今日の復習: N問」＋ 総問題数（前回起動時から増えていたら「+N問追加」と表示）
- **出題** … 問題文 →「答えを見る」→ 正答＋別表記＋補足 →「正解 / 惜しい / 不正解」の3ボタンで自己採点。**入力式は使わない。**
- 10問ごとに正答率と弱点タグを短く挟む。
- **結果** … 正答率 / 弱点タグ / 間違えた問題一覧 /「エクスポート」「インポート」ボタン。

## 9. UI

- 片手操作前提。主要ボタンは画面下 1/3 に大きく配置。
- ダークモード固定。目が疲れないコントラスト。
- アニメーションは最小限。1問あたり最短タップ数で進む。

## 10. オフライン対応

- Service Worker で `index.html` と `data/questions.json` をキャッシュ。一度開けば圏外でも解ける。
- 起動時、ネットがあれば `questions.json` を `cache: 'no-cache'` で取り直して更新を反映。失敗したらキャッシュを使う。
- 更新が反映されない事故を防ぐため、`version` を画面隅に常時表示する（右上に「データ vN / アプリ vN」）。
- **`index.html` を変更したら `sw.js` の `CACHE` の番号を必ず +1 する。** これを忘れると端末に古い画面が residual で残る。
- `data/` 配下はネットワーク優先（失敗時キャッシュ）、それ以外はキャッシュ優先＋裏で更新。

## 11. 進め方

- Phase 1: リポジトリ作成 → CLAUDE.md と data/ の雛形 → プリント写真の送り方を説明
- Phase 2: プリント1枚 → `questions.json` に追記 → 内容確認
- Phase 3: `index.html` と `sw.js` 作成 → GitHub Pages 公開 → URL ✅ 完了
- Phase 4: 実機で動作確認 → 修正

勝手に先のフェーズへ進まない。公開手順を案内するときは、**1ステップずつ**出してユーザーの完了報告を待つ。
